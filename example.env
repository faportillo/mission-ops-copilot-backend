# Example environment configuration for Mission Ops Copilot
# Copy this file to ".env" and adjust values as needed.

# Runtime
NODE_ENV=development
LOG_LEVEL=info
PORT=3000

# Data backend: one of "in-memory" | "file" | "postgres"
# - in-memory: volatile, no persistence
# - file: JSON files under DATA_DIR
# - postgres: uses Prisma with DATABASE_URL
DATA_BACKEND=in-memory

# File backend settings
# Only required when DATA_BACKEND=file
# DATA_DIR=./data

# Postgres backend settings
# Only required when DATA_BACKEND=postgres
# Local CLI / tools:
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mission_ops?schema=app



# Postgres container credentials (used by docker-compose)
POSTGRES_DB=mission_ops
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

# Kafka settings
# Local host access (outside Docker): use localhost:29092 (published port)
KAFKA_BROKERS=localhost:29092
# Inside docker-compose network, services reach Kafka via its service name "kafka"
# Used as default in docker-compose for the app container:
# KAFKA_BROKERS_APP_CONTAINER=kafka:9092
KAFKA_CLIENT_ID=mission-ops
KAFKA_GROUP_ID=mission-ops-consumers

